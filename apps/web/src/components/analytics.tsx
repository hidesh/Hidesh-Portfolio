'use client';

import { useEffect } from 'react';

export function Analytics() {
  useEffect(() => {
    // Initialize analytics tracking
    // This will be implemented when we build the analytics system
  }, []);

  return null;
}